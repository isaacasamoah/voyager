# Production Environment Variables
# Copy these to your Vercel/deployment platform environment variables

# Database (use production PostgreSQL URL)
DATABASE_URL="postgresql://user:password@production-host:5432/ai_career_coach?sslmode=require"

# NextAuth (IMPORTANT: Use your production domain)
NEXTAUTH_URL="https://your-production-domain.vercel.app"
NEXTAUTH_SECRET="generate-new-secret-for-production"

# Google OAuth (Update redirect URI in Google Console to production domain)
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# LinkedIn OAuth (Update redirect URL in LinkedIn settings to production domain)
LINKEDIN_CLIENT_ID="your-linkedin-client-id"
LINKEDIN_CLIENT_SECRET="your-linkedin-client-secret"

# OpenAI
OPENAI_API_KEY="sk-..."

# Production checklist:
# ✅ Generated new NEXTAUTH_SECRET (never reuse dev secret)
# ✅ Updated NEXTAUTH_URL to production domain
# ✅ Updated OAuth redirect URIs in provider settings
# ✅ Verified DATABASE_URL has SSL enabled (?sslmode=require)
# ✅ All secrets are properly secured and not committed to git
