name: mars-mission-planner
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  # Current dependencies for Phase 2
  - pip:
    - streamlit>=1.28.0
    - openai>=1.0.0
    - anthropic>=0.69.0
    - python-dotenv>=1.0.0
    # Next dependencies for Session 2/3
    - langchain>=0.1.0
    - langchain-openai>=0.0.2
    - numpy>=1.24.0
    # Development and testing tools
    - pytest
    - black
    - flake8